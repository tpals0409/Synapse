# Fixture — target dev doc PASS case

## 1. Goal
화면 표현이 모두 표에 존재.

## 2. Deliverable & Receipt

## 3. Scope
**In:**
- Onboarding 단일 화면 (디자인 목업 `screens.jsx` `OnboardingScreen` 1:1).
- FirstChat 화면 (입력창 + 메시지 리스트).
- GhostHint 등장 시점 (recall-l1 — Sprint 3).

**Out:**
- 다른 모든 화면.

## 4. Architecture
- (생략)
