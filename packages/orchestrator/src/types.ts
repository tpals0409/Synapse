// DecisionAct 4 원 enum 동결 단일 출처 = `@synapse/protocol`.
// memory: decision_orchestrator_enum.md, Sprint 3 carry-over 2.
// 본 모듈은 protocol re-export 만 — 변경 시 carry-over 2 위반.
export type { DecisionAct } from '@synapse/protocol';
