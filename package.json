{
  "name": "synapse",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "receipt:0": "bash scripts/receipt/sprint-0.sh",
    "export:sprint8": "pnpm --filter @synapse/export-pipeline exec node sprint8-data.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
